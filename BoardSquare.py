class BoardSquare:
    def __init__(self, x_pos, y_pos, size, position, piece, is_white):
        self.x_pos = x_pos
        self.y_pos = y_pos
        self.size = size
        self.position = position
        self.piece = piece
        self.is_white = is_white
